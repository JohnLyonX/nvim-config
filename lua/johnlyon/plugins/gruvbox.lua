return {
  "ellisonleao/gruvbox.nvim",
  lazy = true,
  opts = {
    contrast = "hard", -- "" = medium(默认) / "hard" 深背景 / "soft" 浅背景
    italic = {
      strings = false,
      comments = true,
      operators = false,
      folds = true,
    },
    transparent_mode = false,
  },
}
